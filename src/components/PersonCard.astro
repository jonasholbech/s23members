---
const { image, name, email } = Astro.props;
---

<article>
  <img src={image} alt="profilbillede" />
  <div>
    <h2>{name}</h2>
    <p>{email}</p>
  </div>
</article>

<style>
  a {
    text-decoration: none;
    color: inherit;
  }
  article {
    border-radius: 1em;
    overflow: hidden;
    background-color: var(--brandbg);
    color: white;
    height: 410px;
    display: flex;
    flex-direction: column;
    align-items: baseline;
    justify-content: space-between;
  }

  div {
    padding-left: 1em;
  }
</style>
